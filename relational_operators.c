#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i=4,j=5,r;
    r=i>j;                   //The result of logical operators is always either 0 (FALSE) or 1 (TRUE)
    printf("%d",r);
    return 0;
}
