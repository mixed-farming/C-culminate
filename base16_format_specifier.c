#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;

    printf("Enter the decimal number which is to be converted to the hexadecimal form : ");
    scanf("%d",&a);

    printf("The hexadecimal value of %d is %x",a,a);
    return 0;
}
